// This file is deprecated as the app has moved to local recognition.
// Keeping an empty export to avoid build errors if referenced elsewhere, 
// though all references should be updated.
export const generateQuestionsFromInput = async () => {
    throw new Error("Remote API is disabled.");
};
